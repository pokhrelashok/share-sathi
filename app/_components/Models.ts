export type User = {
  dp: string;
  username: string;
  password: string;
  crn: string;
  pin: string;
  name: string;
  id: string | null;
  bank: string;
};

export type Capital = {
  id: string;
  name: string;
  code: string;
};
